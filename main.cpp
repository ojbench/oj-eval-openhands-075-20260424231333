#include <bits/stdc++.h>
#include "str.hpp"
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // No I/O required for this task; provide empty main for compilation.
    return 0;
}
